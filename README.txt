Al Language
===========

Al is a dynamic, compiled language.  At present it works by translating code to 
C++.  It's sytnax and semantics are very similar to Python.

It currently requires the following to compile the generated C++ code:
    * Boehm GC
    * GMP
